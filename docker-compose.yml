# version: '3'
services:
  web:
    build: .
    image: rails_template:latest
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - '80:3000'
    environment:
      - RAILS_ENV=production
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - DATABASE_URL=postgresql://postgres:twitter-rds-2026@twitter-clone-db.cl0cm0o8yoe9.ap-northeast-1.rds.amazonaws.com:5432/twitter_clone_rds
